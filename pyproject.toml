[project]
name = "centralized-format-uploader"
version = "0.1.0"
description = "centralized-format-uploader plugin"
requires-python = ">=3.10"
dependencies = [
    "synapse-sdk",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "ruff>=0.8",
]
